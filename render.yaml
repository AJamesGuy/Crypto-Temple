services:
  - type: web
    name: CryptoTemple-Backend
    runtime: python-3.11
    buildCommand: pip install -r Backend/requirements.txt
    startCommand: cd Backend && gunicorn flask_app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
